<div id="chat_{{id}}">
    <div class="message-actions">
        <a href="#" hx-post="/chat/message" hx-trigger="click" hx-target="#chat_{{id}}" hx-swap="outerHTML"
            hx-include="#chat_{{id}}" hx-vals='{"id":"{{id}}"}'>
            ✓ save
        </a>
    </div>
    <span class="role-indicator">{{ role }}</span>
    <textarea class="full" name="content" autocomplete="off" spellcheck="false"
        autocorrect="off" autocapitalize="off">{{ content }}</textarea>
    <input type="hidden" name="role" value="{{ role }}" />
</div>